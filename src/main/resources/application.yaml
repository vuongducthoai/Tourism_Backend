server:
  port: 8080
spring:
  datasource:
    url: ${DBMS_CONNECTION:jdbc:mysql://localhost:3306/tourism}
    driverClassName: "com.mysql.cj.jdbc.Driver"
    password: ${DBMS_PASSWORD:Thoai12309@}
    username: ${DBMS_USERNAME:root}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: thoai12309@gmail.com
#    password: kwmttdbfyzxniqdc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
app:
  frontend:
    url: http://localhost:3000/reset-password
